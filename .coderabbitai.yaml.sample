# これは Coderabbit AI の設定ファイルのサンプルです。
# この設定を使用するには、このファイルをリポジトリのルートに `.coderabbitai.yaml` としてコピーしてください。

# LLM のレスポンスの言語
# ISO 言語コードを指定する、デフォルトは "ja-JP"
language: "ja-JP"

# ファイルレベルの制御
skip_simple_changes: true # シンプルな変更をスキップするか (デフォルト: false)
review_diff_since_last_review: false # 最後の AI レビュー以降の差分のみをレビュー対象とするか (デフォルト: false)

# ファイルフィルター設定
file_filter:
  exclude: # レビュー対象外のファイルパス (Globパターン, 除外専用)
    - "dist/**"
    - "**/*.min.js"
    - "**/*.map"
    - "deno.lock"
    - "yarn.lock"
  max_changes: 50 # レビュー対象とするファイルの最大変更行数 (0の場合は無制限, デフォルト: 50)

# パスごとのレビュー指示
file_path_instructions:
  - path: "src/**/*.{ts,tsx}"
    instructions: |
      TypeScriptコードのレビュー指示：
      - 型の適切な使用
      - nullチェックの完全性
      - 非同期処理の適切な実装
  - path: "test/**/*.ts"
    instructions: |
      テストコードのレビュー指示：
      - テストケースの網羅性
      - エッジケースの考慮
      - テストの独立性確保
